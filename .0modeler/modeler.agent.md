---
name: .0modeler
source: .claude/agents/modeler.md
---

# .0modeler Agent Prompt (Example Copy)

This file shows where to place the **agent prompt** for `.0modeler`.

The canonical, fully maintained definition for this agent lives in:

- `.claude/agents/modeler.md`

You can **copy or adapt** the content from that file into here if you want a standalone prompt inside the `.0modeler/` directory. For now, treat this file as an example pointer so that:

- humans and tools can quickly discover
- "this is where modeler.agent prompt goes" inside the `.0modeler/` folder.
